# Changelog

All notable changes to this project are documented in this file.

## [Unreleased] - 2026-07-04
- Refactor wording from "Prediction" to "Assessment" across UI and documentation.
- Updated API responses to include `assessment` / `assessment_label` and `engine_used`.
- Renamed internal helpers and docstrings for clarity (`make_assessment`).
- Added smoke tests: `tests/test_api.py`, `tests/test_ui_submit.py` and saved result snapshot `tests/result_page.html`.
- Updated `README.md`, `notebooks/train_model.py`, and `requirements.txt` to reflect tone change.

